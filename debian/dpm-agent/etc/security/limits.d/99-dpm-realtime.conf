# Allow the dpm service user to use realtime scheduling.
# Note: PAM limits may not apply to systemd services (which use LimitRTPRIO instead).
# This file covers interactive/foreground runs of dpm-agent.
dpm   soft    rtprio  99
dpm   hard    rtprio  99
