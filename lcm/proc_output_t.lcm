package dpm_msgs;

struct proc_output_t
{
    // DPM wire-protocol version — consumers drop messages with a mismatching
    // version. See dpm.constants.DPM_PROTOCOL_VERSION.
    int32_t protocol_version;

    int64_t timestamp;
    string name;
    string hostname;
    string group;
    string stdout;
    string stderr;
}
